# Stripe — create at https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Stripe webhook signing secret — from the webhook endpoint you create at
# https://dashboard.stripe.com/webhooks (event: payment_intent.succeeded).
# For local testing: `stripe listen --forward-to localhost:1500/api/webhook`
STRIPE_WEBHOOK_SECRET=whsec_...

# Order notification — where the owner copy of each order confirmation lands
# on payment_intent.succeeded. These are INTERNAL inboxes (the owners), not a
# customer-facing address. Comma-separate for multiple recipients.
ORDER_NOTIFICATION_EMAIL=you@example.com,partner@example.com

# Mailgun — sends the order confirmation (owner + BCC customer).
# API key: https://app.mailgun.com/app/account/security/api_keys (Private API key)
#
# MAILGUN_FROM must stay on the mg.* subdomain — that is what is DNS-verified
# for sending. It does NOT receive mail, so it is deliberately not the address
# customers write to. Replies are steered to CONTACT_EMAIL (src/lib/site.ts)
# via a Reply-To header in src/lib/mailgun.ts.
MAILGUN_API_KEY=
MAILGUN_DOMAIN=mg.theslimecompany.com
MAILGUN_FROM=The Slime Co <orders@mg.theslimecompany.com>

# BetterStack Telemetry — structured application logs (src/lib/logger.ts).
# Both unset is a supported state: logging falls back to console only, which
# is what local dev and preview deploys normally want.
# Source token is per-source and write-only; find it under the source in
# https://telemetry.betterstack.com/. Vault: op://DEV/dd.betterstack.slime
BETTERSTACK_SOURCE_TOKEN=
BETTERSTACK_INGEST_HOST=sNNNNNNN.us-west-2a.betterstackdata.com

# Google Analytics 4 — e-commerce funnel + Google Ads/Merchant Center linkage.
# Measurement ID from https://analytics.google.com/ (Admin > Data streams).
# Unset disables GA4 entirely, which is what local dev and previews want.
#
# NEXT_PUBLIC_ IS BAKED IN AT BUILD TIME — setting it in Vercel is only half
# the job, the deploy has to be rebuilt before it takes effect.
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# GA4 Measurement Protocol secret, for the SERVER-SIDE purchase event sent from
# the Stripe webhook (src/lib/ga-measurement-protocol.ts). Create under the
# same data stream: Measurement Protocol API secrets > Create.
#
# Deliberately NOT NEXT_PUBLIC_ — anyone holding this can write arbitrary
# events, including fake revenue, into the property. Unset means GA4 records no
# purchases at all: the browser never sends one, on purpose.
GA_API_SECRET=

# Set to 1 to route Measurement Protocol calls to Google's VALIDATING endpoint,
# which returns what is wrong with the payload. The normal endpoint answers 204
# to anything, valid or not, so this is the only way to debug it.
# It validates WITHOUT recording — never leave it on in production.
GA_DEBUG_MP=

# Plausible — cookieless traffic + revenue analytics. The bare domain as
# registered in Plausible (no scheme, no www). Vault: op://DEV/dd.plausible.slime
# Unset disables it.
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=theslimecompany.com

# Search engine ownership verification, rendered as meta tags by layout.tsx.
# Google: Search Console > Add property > HTML tag (the content="..." value only).
# Bing:   Bing Webmaster Tools > Import from GSC, or the meta-tag method.
# Both are read at BUILD time — paste, then redeploy.
GOOGLE_SITE_VERIFICATION=
BING_SITE_VERIFICATION=

# DD print worker — pipes ZPL shipping labels to the Zebra 4x6 on dd-mini.
# Service lives in ~/dev/dd/infra/print-worker (owned by the dd session, not
# this repo) and is published through Jason's cloudflared tunnel.
#
# PRINT_WORKER_TOKEN is a SHARED SECRET and is deliberately NOT prefixed
# NEXT_PUBLIC_. src/lib/print.ts is server-only: an admin screen that wants to
# print calls one of our API routes, and that route calls the worker. If this
# value ever reaches the browser bundle it is burned and buys nothing.
#
# Vault: op://DEV/dd.printworker/credential — CONFIRMED against the real item
# 2026-09-08 (API Credential category, 32 random bytes hex). The same secret is
# read by the worker itself on dd-mini, so the two sides must move together:
# rotating it means updating 1Password, the worker's launchd env, and this
# deploy's env, or every print 401s.
#
# Leave both unset to disable printing — callers throw a clear "not configured"
# rather than silently no-op.
PRINT_WORKER_URL=https://print.dosscodesigns.com
PRINT_WORKER_TOKEN=

# Shippo — buys USPS labels and returns ZPL for the Zebra 4x6.
# Pirate Ship has no public API; Shippo sits on the same USPS Commercial Plus
# tier at $0.05/label. Vault: op://DEV/dd.shippo.slime/credential
#
# A shippo_test_ key quotes and "buys" without real postage — src/lib/shipping.ts
# exposes isTestMode() off that prefix. SWAPPING TO A LIVE KEY MEANS EVERY
# --buy SPENDS REAL MONEY. Server-only: no NEXT_PUBLIC_ prefix, ever.
SHIPPO_API_KEY=shippo_test_...

# Ship-from address, printed on every label.
# EMAIL AND PHONE ARE REQUIRED, not optional decoration: USPS rejects the label
# purchase with "Seller info missing email or phone" — and only at the purchase
# step, after rates have already come back looking healthy. shipFromAddress()
# therefore fails fast if either is unset.
SHIP_FROM_NAME=The Slime Co
SHIP_FROM_COMPANY=Dossco Designs LLC
SHIP_FROM_STREET1=
SHIP_FROM_STREET2=
SHIP_FROM_CITY=
SHIP_FROM_STATE=FL
SHIP_FROM_ZIP=
SHIP_FROM_COUNTRY=US
SHIP_FROM_EMAIL=info@theslimecompany.com
SHIP_FROM_PHONE=

# Admin panel (/admin) — order fulfilment and coupon usage.
# The panel shows every customer's name, address and email and can spend money
# buying postage, so both of these must be set for it to work at all. Unset
# ADMIN_PASSWORD denies every login rather than admitting everyone, and an
# ADMIN_SESSION_SECRET under 32 chars refuses to issue sessions.
#
# Generate the secret with: openssl rand -hex 32
ADMIN_PASSWORD=
ADMIN_SESSION_SECRET=
